import { createFileRoute, Link } from "@tanstack/react-router";
import { useEffect, useRef, useState, type ReactNode } from "react";

import heroPortrait from "@/assets/hero-portrait.jpg";
import aboutPortrait from "@/assets/about-portrait.jpg";
import projIrctc from "@/assets/proj-irctc.jpg";
import projAthira from "@/assets/proj-athira.jpg";
import projEsummit from "@/assets/proj-esummit.jpg";
import projNoosphere from "@/assets/proj-noosphere.jpg";
import brandCafe from "@/assets/brand-cafe.jpg";
import brandCec from "@/assets/brand-cec.jpg";
import brandMerch from "@/assets/brand-merch.jpg";
import brandSocial from "@/assets/brand-social.jpg";
import brandPoster from "@/assets/brand-poster.jpg";
import lahorizeeracover from "@/assets/lahorizeeracover.png";

import eSummitPreview from "@/assets/3sliderpost.jpg";
import beyondTravel from "@/assets/beyond-travel.jpg";
import beyondSketch from "@/assets/beyond-sketch.jpg";
import beyondCampus from "@/assets/beyond-campus.jpg";
import beyondBooks from "@/assets/beyond-books.jpg";
import beyondCoffee from "@/assets/beyond-coffee.jpg";
import beyondEvents from "@/assets/beyond-events.jpg";

export const Route = createFileRoute("/")({
  head: () => ({
    meta: [
      { title: "Zoya Shaikh - Product Designer & Strategist" },
      {
        name: "description",
        content:
          "Portfolio of Zoya Shaikh - product design, strategy, brand identity and visual design. Civil Engineering at IIT Roorkee.",
      },
      { property: "og:title", content: "Zoya Shaikh - Product Designer & Strategist" },
      {
        property: "og:description",
        content:
          "Designing products, brands and experiences people remember. Selected work by Zoya Shaikh.",
      },
    ],
  }),
  component: Portfolio,
});

/* ------------------------------ helpers ------------------------------ */

function useReveal<T extends HTMLElement>(threshold = 0.12) {
  const ref = useRef<T | null>(null);
  const [shown, setShown] = useState(false);
  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    const io = new IntersectionObserver(
      (entries) => {
        for (const e of entries) {
          if (e.isIntersecting) {
            setShown(true);
            io.disconnect();
            break;
          }
        }
      },
      { threshold, rootMargin: "0px 0px -40px 0px" },
    );
    io.observe(el);
    return () => io.disconnect();
  }, [threshold]);
  return { ref, shown };
}

function Reveal({
  children,
  delay = 0,
  as: As = "div",
  className = "",
}: {
  children: ReactNode;
  delay?: number;
  as?: "div" | "section" | "span" | "p" | "h1" | "h2" | "h3";
  className?: string;
}) {
  const { ref, shown } = useReveal<HTMLDivElement>();
  const Cmp = As as any;
  return (
    <Cmp
      ref={ref}
      className={`reveal ${shown ? "revealed" : ""} ${className}`}
      style={{ transitionDelay: `${delay}ms` }}
    >
      {children}
    </Cmp>
  );
}

/* ------------------------------ page ------------------------------ */

function Portfolio() {
  return (
    <main className="bg-background text-foreground font-sans">
      <Nav />
      <Hero />
      <Manifesto />
      <ProductWork />
      <BrandWork />
      <EditorialWork />
      <About />
      <Experience />
      <Skills />
      <Footer />

    </main>
  );
}

/* ------------------------------ nav ------------------------------ */

function Nav() {
  return (
    <header className="fixed inset-x-0 top-0 z-50 bg-background/70 backdrop-blur-md">
      <div className="mx-auto flex max-w-[1440px] items-center justify-between px-6 py-5 md:px-10">
        <a href="#top" className="font-display text-[22px] leading-none">
          Zoya <span className="italic text-muted-foreground">Shaikh</span>
        </a>
        <nav className="hidden gap-9 text-[13px] text-muted-foreground md:flex">
          <a href="#work" className="link-underline"><span className="link-underline-inner hover:text-foreground">Product</span></a>
          <a href="#brand" className="link-underline"><span className="link-underline-inner hover:text-foreground">Brand</span></a>
          <a href="#editorial" className="link-underline"><span className="link-underline-inner hover:text-foreground">Editorial</span></a>
          <a href="#about" className="link-underline"><span className="link-underline-inner hover:text-foreground">About</span></a>
          <a href="#experience" className="link-underline"><span className="link-underline-inner hover:text-foreground">Experience</span></a>
          <a href="#contact" className="link-underline"><span className="link-underline-inner hover:text-foreground">Contact</span></a>

        </nav>
        <a
          href="#"
          className="hidden items-center gap-2 rounded-full border border-foreground/25 px-4 py-2 text-[12px] tracking-wide transition-colors hover:bg-foreground hover:text-background md:inline-flex"
        >
          Résumé <span aria-hidden className="text-[10px]">↓</span>
        </a>
      </div>
    </header>
  );
}

/* ------------------------------ hero ------------------------------ */

function Hero() {
  return (
    <section id="top" className="relative overflow-hidden pt-32 md:pt-40">
      <div className="mx-auto grid max-w-[1440px] grid-cols-1 gap-16 px-6 pb-24 md:grid-cols-12 md:gap-10 md:px-10 md:pb-32">
        {/* Left */}
        <div className="md:col-span-7 md:pt-6">
          <Reveal>
            <p className="eyebrow">Portfolio - Vol. I · 2026</p>
          </Reveal>
          <Reveal delay={120}>
            <h1 className="font-display mt-8 text-[52px] leading-[0.98] tracking-tight sm:text-[72px] md:text-[92px] lg:text-[108px]">
              Designing
              <br />
              products,
              <br />
              <span className="italic text-muted-foreground">brands</span> &amp;
              <br />
              experiences
              <br />
              people remember.
            </h1>
          </Reveal>
          <Reveal delay={260}>
            <p className="mt-10 max-w-md text-[15px] leading-relaxed text-muted-foreground">
              I&apos;m Zoya - a product designer and strategist studying Civil Engineering at
              IIT Roorkee. I work at the quiet intersection of product thinking, interface craft
              and brand identity, helping teams turn ambiguous problems into calm, considered
              experiences.
            </p>
          </Reveal>
          <Reveal delay={380}>
            <div className="mt-10 flex flex-wrap items-center gap-4">
              <a
                href="#work"
                className="group inline-flex items-center gap-3 rounded-full bg-foreground px-6 py-3.5 text-[13px] tracking-wide text-background transition-transform hover:-translate-y-[1px]"
              >
                View Work
                <span className="transition-transform group-hover:translate-x-1">→</span>
              </a>
              <a
                href="#"
                className="inline-flex items-center gap-3 rounded-full border border-foreground/20 px-6 py-3.5 text-[13px] tracking-wide text-foreground transition-colors hover:bg-foreground/[0.04]"
              >
                Download Resume
                <span className="text-muted-foreground">PDF</span>
              </a>
            </div>
          </Reveal>
        </div>

        {/* Right - art-directed editorial collage */}
        <div className="relative md:col-span-5">
          <Reveal delay={200}>
            <div className="relative mx-auto h-[560px] w-full max-w-[460px] sm:h-[620px]">

              {/* Portrait - anchor of the composition */}
              <div className="absolute left-1/2 top-6 z-20 aspect-[4/5] w-[74%] -translate-x-1/2 overflow-hidden bg-paper-2 shadow-[0_30px_60px_-30px_rgba(20,15,10,0.28)]">
                <img
                  src={heroPortrait}
                  alt="Portrait of Zoya Shaikh"
                  width={1024}
                  height={1280}
                  className="h-full w-full object-cover grayscale contrast-[1.02]"
                />
              </div>

              {/* Card in front - Athira (right side, mid-depth) */}
              <figure
                className="absolute -right-2 top-[46%] z-30 w-[160px] rotate-[6deg] bg-paper p-2 shadow-[0_28px_55px_-24px_rgba(20,15,10,0.4)] sm:-right-4 sm:w-[186px]"
              >
                <img
                  src={projAthira}
                  alt="Athira wellness app"
                  className="aspect-[4/5] w-full object-cover"
                  loading="eager"
                />
                <figcaption className="px-1 pb-1 pt-2">
                  <div className="eyebrow text-[9px]">App · Wellness</div>
                  <div className="font-display text-[14px] leading-tight">Athira</div>
                </figcaption>
              </figure>

              {/* Card in front - E-Summit (bottom-left, closest layer) */}
              <figure
                className="absolute -bottom-2 left-4 z-40 w-[200px] -rotate-[4deg] bg-paper p-2 shadow-[0_32px_60px_-24px_rgba(20,15,10,0.45)] sm:w-[228px]"
              >
                <img
                  src={projEsummit}
                  alt="E-Summit brand identity"
                  className="aspect-[5/4] w-full object-cover"
                  loading="eager"
                />
                <figcaption className="flex items-baseline justify-between gap-2 px-1 pb-1 pt-2">
                  <div className="font-display text-[14px] leading-tight">E-Summit</div>
                  <div className="eyebrow text-[9px]">Brand Identity</div>
                </figcaption>
              </figure>
            </div>
          </Reveal>
        </div>

      </div>

      {/* meta strip */}
      <div className="border-t border-line">
        <div className="mx-auto grid max-w-[1440px] grid-cols-2 gap-6 px-6 py-6 text-[12px] text-muted-foreground md:grid-cols-4 md:px-10">
          <div><span className="eyebrow block">Based in</span><span className="mt-1 block text-foreground">Roorkee, India</span></div>
          <div><span className="eyebrow block">Studying</span><span className="mt-1 block text-foreground">Civil Engineering, IIT Roorkee</span></div>
          <div><span className="eyebrow block">Focus</span><span className="mt-1 block text-foreground">Product · Brand · Strategy</span></div>
          <div><span className="eyebrow block">Status</span><span className="mt-1 block text-foreground">Selectively taking work</span></div>
        </div>
      </div>
    </section>
  );
}

/* ------------------------------ manifesto ------------------------------ */

function Manifesto() {
  return (
    <section className="border-t border-line">
      <div className="mx-auto max-w-[1440px] px-6 py-32 md:px-10 md:py-48">
        <Reveal>
          <p className="eyebrow mb-10">A quiet belief</p>
        </Reveal>
        <Reveal delay={120}>
          <h2 className="font-display text-[44px] leading-[1.02] sm:text-[64px] md:text-[92px] lg:text-[112px]">
            The best products don&apos;t
            <br />
            <span className="italic text-muted-foreground">announce themselves.</span>
            <br />
            They quietly make the next
            <br />
            step feel obvious.
          </h2>
        </Reveal>
      </div>
    </section>
  );
}

/* ------------------------------ product work ------------------------------ */

type Project = {
  slug: string;
  no: string;
  title: string;
  category: string;
  story: string;
  tags: string[];
  image: string;
  ratio: string;
};

const products: Project[] = [
  {
    slug: "irctc-ai-copilot",
    no: "01",
    title: "IRCTC AI Copilot",
    category: "Product design · Conversational AI",
    story:
      "Rethinking India's largest booking system as a single, conversational surface - one that quietly replaces twelve broken flows with a copilot that knows how travel actually works in this country.",
    tags: ["Product Strategy", "Conversational UX", "Design System", "Prototype"],
    image: projIrctc,
    ratio: "aspect-[5/4]",
  },
  {
    slug: "athira",
    no: "02",
    title: "Athira",
    category: "0 → 1 · Consumer wellness",
    story:
      "A slow, unhurried mindfulness app built for people who are tired of being nudged. Athira is a study in restraint - soft type, considered pacing, and a product model that earns attention instead of stealing it.",
    tags: ["0 → 1 Design", "Brand", "iOS", "Motion"],
    image: projAthira,
    ratio: "aspect-[5/4]",
  },
  {
    slug: "noosphere",
    no: "03",
    title: "Noosphere",
    category: "SaaS · Knowledge platform",
    story:
      "A reading and thinking companion for people who take ideas seriously. Designed around a single principle - the interface should disappear the moment the user starts to think.",
    tags: ["Web App", "Information Design", "Design System", "Research"],
    image: projNoosphere,
    ratio: "aspect-[5/4]",
  },
];

function ProductWork() {
  return (
    <section id="work" className="border-t border-line bg-paper">
      <div className="mx-auto max-w-[1440px] px-6 pt-24 md:px-10 md:pt-32">
        <div className="flex items-end justify-between gap-8">
          <Reveal>
            <div>
              <p className="eyebrow">Section 03 - Selected Work</p>
              <h2 className="font-display mt-6 text-[40px] leading-[1] md:text-[64px]">
                Product Design
                <br />
                <span className="italic text-muted-foreground">&amp; Strategy</span>
              </h2>
            </div>
          </Reveal>
          <Reveal delay={120}>
            <p className="hidden max-w-xs text-[14px] leading-relaxed text-muted-foreground md:block">
              Three projects, chosen not for how they look but for how they think.
              Each began as a business problem and ended as a considered product decision.
            </p>
          </Reveal>
        </div>

        <div className="mt-24 space-y-32 md:mt-32 md:space-y-48">
          {products.map((p, i) => (
            <ProjectRow key={p.no} project={p} reversed={i % 2 === 1} />
          ))}
        </div>
      </div>
    </section>
  );
}

function ProjectRow({ project, reversed }: { project: Project; reversed: boolean }) {
  return (
    <article className="grid grid-cols-1 items-center gap-12 md:grid-cols-12 md:gap-16">
      <Reveal
        className={`md:col-span-7 ${reversed ? "md:order-2" : ""}`}
      >
        <div className={`relative ${project.ratio} w-full overflow-hidden bg-paper-2`}>
          <img
            src={project.image}
            alt={project.title}
            className="h-full w-full object-cover"
            loading="lazy"
          />
        </div>
      </Reveal>

      <Reveal
        delay={120}
        className={`md:col-span-5 ${reversed ? "md:order-1 md:pr-6" : "md:pl-6"}`}
      >
        <div className="flex items-baseline gap-6 text-muted-foreground">
          <span className="font-display text-[28px] leading-none">{project.no}</span>
          <span className="eyebrow">{project.category}</span>
        </div>
        <h3 className="font-display mt-6 text-[40px] leading-[1] md:text-[56px]">
          {project.title}
        </h3>
        <p className="mt-6 max-w-md text-[15px] leading-relaxed text-muted-foreground">
          {project.story}
        </p>
        <ul className="mt-8 flex flex-wrap gap-2">
          {project.tags.map((t) => (
            <li
              key={t}
              className="rounded-full border border-foreground/15 px-3 py-1.5 text-[11px] tracking-wide text-muted-foreground"
            >
              {t}
            </li>
          ))}
        </ul>
        <Link
          to="/projects/$slug"
          params={{ slug: project.slug }}
          className="link-underline mt-10 inline-flex items-center gap-2 text-[13px] tracking-wide"
        >
          <span className="link-underline-inner">Read the case study</span>
          <span aria-hidden>→</span>
        </Link>
      </Reveal>
    </article>
  );
}

/* ------------------------------ brand work ------------------------------ */

function BrandWork() {
  return (
    <section id="brand" className="border-t border-line">
      <div className="mx-auto max-w-[1440px] px-6 py-24 md:px-10 md:py-32">
        <div className="flex items-end justify-between gap-8">
          <Reveal>
            <div>
              <p className="eyebrow">Section 04 - A gallery</p>
              <h2 className="font-display mt-6 text-[40px] leading-[1] md:text-[64px]">
                Brand <span className="italic text-muted-foreground">&amp;</span>
                <br />
                Visual Design
              </h2>
            </div>
          </Reveal>
          <Reveal delay={120}>
            <p className="hidden max-w-xs text-[14px] leading-relaxed text-muted-foreground md:block">
              Identities, campaigns and printed objects - curated as a small
              editorial spread rather than a wall of thumbnails.
            </p>
          </Reveal>
        </div>

        {/* Editorial magazine grid */}
        <div className="mt-20 grid grid-cols-12 gap-4 md:mt-28 md:gap-6">
          <Reveal className="col-span-12 md:col-span-8">
            <BrandCard slug="e-summit-identity" image={eSummitPreview} label="Brand Identity" title="E-Summit - IIT Roorkee" ratio="aspect-[16/10]" />
          </Reveal>
          <Reveal delay={120} className="col-span-6 md:col-span-4">
            <BrandCard slug="e-summit-2026" image="/assets/100dayss-B5LqMzPy.png" label="Countdown" title="100 Days to Go" ratio="aspect-[4/5]" />
          </Reveal>

          <Reveal className="col-span-6 md:col-span-4">
            <BrandCard slug="e-summit-2026" image="/assets/esummitidentity-BCxZkC8M.png" label="Logo" title="Transcending the Paradigm" ratio="aspect-[4/5]" />
          </Reveal>
          <Reveal delay={120} className="col-span-12 md:col-span-5">
            <BrandCard slug="e-summit-2026" image="/assets/ev1-DZ0pJQgf.png" label="Event Promotion" title="Event Itinerary" ratio="aspect-[5/4]" />
          </Reveal>
          <Reveal delay={200} className="col-span-6 md:col-span-3">
            <BrandCard slug="e-summit-2026" image="/assets/b1-4VC_6b1W.png" label="Banners" title="On-ground Branding" ratio="aspect-[4/5]" />
          </Reveal>

          <Reveal className="col-span-12 md:col-span-5">
            <div className="flex h-full flex-col justify-end md:pb-8 md:pl-2">
              <p className="eyebrow">Also, in circulation</p>
              <p className="font-display mt-6 text-[26px] leading-tight md:text-[34px]">
                Feathers, schedules, workshops, passes and banners â€” the visual system
                that keeps the summit recognisable across every touchpoint.
              </p>
            </div>
          </Reveal>
          <Reveal delay={120} className="col-span-12 md:col-span-7">
            <BrandCard slug="e-summit-2026" image="/assets/tickets-DtXGmVyR.png" label="Event Access" title="Tickets Live" ratio="aspect-[16/10]" />
          </Reveal>
        </div>
      </div>
    </section>
  );
}

function BrandCard({
  slug, image, label, title, ratio,
}: { slug: string; image: string; label: string; title: string; ratio: string }) {
  return (
    <Link to="/projects/$slug" params={{ slug }} className="group block">
      <figure>
        <div className={`relative ${ratio} w-full overflow-hidden bg-paper-2`}>
          <img
            src={image}
            alt={title}
            loading="lazy"
            className="h-full w-full object-cover transition-transform duration-[1200ms] group-hover:scale-[1.02]"
          />
        </div>
        <figcaption className="mt-4 flex items-baseline justify-between gap-4">
          <span className="eyebrow">{label}</span>
          <span className="font-display text-[16px] leading-tight text-foreground">{title}</span>
        </figcaption>
      </figure>
    </Link>
  );
}

/* ------------------------------ editorial & social ------------------------------ */

type Editorial = {
  slug: string;
  title: string;
  category: "Carousel" | "Campaign" | "Editorial" | "Infographic" | "Social Media";
  image: string;
  ratio: string; // aspect ratio utility
};

const editorials: Editorial[] = [
  { slug: "labubu-campaign",      title: "Labubu Campaign",      category: "Campaign",     image: brandSocial, ratio: "aspect-[5/4]" },
  { slug: "aviation-industry",    title: "Aviation Industry",    category: "Carousel",     image: brandMerch,  ratio: "aspect-[4/5]" },
  { slug: "lahori-zeera",         title: "Lahori Zeera",         category: "Campaign",     image: lahorizeeracover, ratio: "aspect-[1/1]" },
  { slug: "ecell-social-media",   title: "E-Cell Social Media",  category: "Social Media", image: projEsummit, ratio: "aspect-[4/5]" },
  { slug: "cec-social-media",     title: "CEC Social Media",     category: "Social Media", image: brandCec,    ratio: "aspect-[5/6]" },
  { slug: "recruitment-campaign", title: "Recruitment Campaign", category: "Campaign",     image: brandSocial, ratio: "aspect-[4/5]" },
  { slug: "nss-campaigns",        title: "NSS Campaigns",        category: "Infographic",  image: brandPoster, ratio: "aspect-[1/1]" },
];

function EditorialWork() {
  return (
    <section id="editorial" className="border-t border-line bg-paper-2/40">
      <div className="mx-auto max-w-[1440px] px-6 py-24 md:px-10 md:py-32">
        <div className="flex items-end justify-between gap-8">
          <Reveal>
            <div>
              <p className="eyebrow">Section 05 - Editorial gallery</p>
              <h2 className="font-display mt-6 text-[40px] leading-[1] md:text-[64px]">
                Editorial <span className="italic text-muted-foreground">&amp;</span>
                <br />
                Social Design
              </h2>
            </div>
          </Reveal>
          <Reveal delay={120}>
            <p className="hidden max-w-xs text-[14px] leading-relaxed text-muted-foreground md:block">
              Campaigns, editorial storytelling, educational carousels and
              social-first visual communication - designed to simplify ideas
              and hold attention a moment longer.
            </p>
          </Reveal>
        </div>

        {/* Masonry - gracefully supports unlimited entries */}
        <div className="mt-20 columns-1 gap-6 sm:columns-2 md:mt-28 md:gap-8 lg:columns-3">
          {editorials.map((e, i) => (
            <Reveal key={e.title} delay={(i % 3) * 90} className="mb-6 break-inside-avoid md:mb-8">
              <EditorialCard item={e} />
            </Reveal>
          ))}
        </div>
      </div>
    </section>
  );
}

function EditorialCard({ item }: { item: Editorial }) {
  return (
    <Link to="/projects/$slug" params={{ slug: item.slug }} className="group block">
      <figure>
        <div className={`relative ${item.ratio} w-full overflow-hidden bg-paper-2`}>
          <img
            src={item.image}
            alt={item.title}
            loading="lazy"
            className="h-full w-full object-cover transition-transform duration-[1200ms] group-hover:scale-[1.02]"
          />
        </div>
        <figcaption className="mt-4 flex items-baseline justify-between gap-4">
          <span className="font-display text-[18px] leading-tight text-foreground">
            {item.title}
          </span>
          <span className="eyebrow shrink-0">{item.category}</span>
        </figcaption>
      </figure>
    </Link>
  );
}

/* ------------------------------ about ------------------------------ */



function About() {
  return (
    <section id="about" className="border-t border-line bg-paper-2/60">
      <div className="mx-auto grid max-w-[1440px] grid-cols-1 gap-16 px-6 py-24 md:grid-cols-12 md:gap-12 md:px-10 md:py-32">
        <Reveal className="md:col-span-5">
          <div className="relative aspect-[4/5] w-full max-w-[440px] overflow-hidden bg-paper">
            <img
              src={aboutPortrait}
              alt="Zoya Shaikh at her desk"
              loading="lazy"
              className="h-full w-full object-cover"
            />
          </div>
        </Reveal>

        <Reveal delay={120} className="md:col-span-7 md:pt-8">
          <p className="eyebrow">Section 05 - About</p>
          <h2 className="font-display mt-6 text-[40px] leading-[1] md:text-[64px]">
            I came to design
            <br />
            <span className="italic text-muted-foreground">the long way around.</span>
          </h2>
          <div className="mt-10 max-w-lg space-y-6 text-[15px] leading-relaxed text-muted-foreground">
            <p>
              I study Civil Engineering at IIT Roorkee - a discipline that trained me to
              respect structure, tolerance and the way small forces move through a system.
              Somewhere between load calculations and site drawings, I noticed that the same
              instincts applied to the digital things I couldn&apos;t stop redrawing on the side.
            </p>
            <p>
              What began as curiosity became a practice. I moved from posters, to interfaces,
              to identity systems, to product decisions - always chasing the same question:
              why does this feel considered, and this one not?
            </p>
            <p>
              Today I work across product design, strategy and brand - not as three separate
              crafts, but as one continuous act of making an idea legible to the people it&apos;s
              meant for.
            </p>
          </div>
        </Reveal>
      </div>
    </section>
  );
}

/* ------------------------------ experience ------------------------------ */

const experience = [
  {
    year: "FEB 2025 - PRESENT",
    role: "Designer → Design Head",
    org: "E-Cell, IIT Roorkee",
    note: "I started at E-Cell as a Designer in February 2025 and was promoted to Design Head in 2026. I lead the design direction and a design team, working across branding, campaigns, digital experiences, event design, merchandise and the E-Summit visual system.",
    caps: "Brand Identity · Product Design · Web · Campaigns · Merchandise · Design Leadership",
  },
  {
    year: "JAN 2026 - APR 2026",
    role: "Product Design Intern",
    org: "Athira",
    note: "Worked across product design and UI/UX, contributing to product experiences, interface work and onboarding-related design.",
    caps: "Product Design · UX · UI · Prototyping · Design Systems",
  },
  {
    year: "FEB 2025 - PRESENT",
    role: "Designer",
    org: "Design Studio",
    note: "A multidisciplinary design role spanning graphic design, branding, product/UI design, Photoshop, visual communication, merchandise design, campaign design and other visual work.",
    caps: "Product · Branding · Visual Design · Editorial · Photoshop · Art Direction",
  },
];

function Experience() {
  return (
    <section id="experience" className="border-t border-line">
      <div className="mx-auto max-w-[1440px] px-6 py-24 md:px-10 md:py-32">
        <div className="grid grid-cols-1 gap-12 md:grid-cols-12 md:gap-16">
          <Reveal className="md:col-span-4">
            <p className="eyebrow">Section 06 - Experience</p>
            <h2 className="font-display mt-6 text-[40px] leading-[1] md:text-[56px]">
              Where I&apos;ve
              <br />
              been building.
            </h2>
            <p className="mt-6 max-w-xs text-[14px] leading-relaxed text-muted-foreground">
              Selected roles that shaped how I design, build and lead.
            </p>
          </Reveal>

          <div className="md:col-span-8">
            <ul className="border-t border-line">
              {experience.map((e, i) => (
                <Reveal key={e.role} delay={i * 80}>
                  <li className="grid grid-cols-12 gap-6 border-t border-line py-8 last:border-b">
                    <div className="col-span-12 md:col-span-3">
                      <p className="eyebrow">{e.year}</p>
                    </div>
                    <div className="col-span-12 md:col-span-9">
                      <div className="flex flex-wrap items-baseline gap-3">
                        <h3 className="font-display text-[24px] leading-tight md:text-[30px]">
                          {e.role}
                        </h3>
                        <span className="text-[14px] italic text-muted-foreground">
                          - {e.org}
                        </span>
                      </div>
                      <p className="mt-3 max-w-xl text-[14px] leading-relaxed text-muted-foreground">
                        {e.note}
                      </p>
                      <p className="mt-4 text-[12px] uppercase tracking-[0.16em] text-muted-foreground">
                        {e.caps}
                      </p>
                    </div>
                  </li>
                </Reveal>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>
  );
}

/* ------------------------------ skills ------------------------------ */

const skills = [
  "Product Design",
  "Product Strategy",
  "Product Management",
  "UX Research",
  "Wireframing",
  "Brand Identity",
  "Graphic Design",
  "Data Analytics",
  "AI",
  "Figma",
  "Illustrator",
  "Photoshop",
  "Canva",
];

const skillGroups = [
  {
    label: "Product",
    items: ["Product Design", "Product Strategy", "Product Management", "UX Research", "Wireframing"],
  },
  {
    label: "Brand & Visual",
    items: ["Brand Identity", "Graphic Design"],
  },
  {
    label: "Tools",
    items: ["Figma", "Illustrator", "Photoshop", "Canva"],
  },
  {
    label: "Data & Tech",
    items: ["Data Analytics", "AI"],
  },
] as const;

function Skills() {
  return (
    <section className="border-t border-line bg-paper-2/60">
      <div className="mx-auto max-w-[1440px] px-6 py-24 md:px-10 md:py-28">
        <div className="grid grid-cols-1 gap-10 md:grid-cols-12 md:gap-16">
          <Reveal className="md:col-span-4">
            <p className="eyebrow">Section 07 - Toolkit</p>
            <h2 className="font-display mt-6 text-[36px] leading-[1] md:text-[44px]">
              A short list
              <br />
              <span className="italic text-muted-foreground">of what I use.</span>
            </h2>
          </Reveal>
          <Reveal delay={120} className="md:col-span-8">
            <div className="grid gap-8 sm:grid-cols-2">
              {skillGroups.map((group) => (
                <div key={group.label} className="space-y-3">
                  <p className="eyebrow">{group.label}</p>
                  <ul className="flex flex-wrap gap-2.5">
                    {group.items.map((skill) => (
                      <li
                        key={skill}
                        className="inline-flex items-center rounded-full border border-foreground/15 bg-background px-4 py-2 text-[13px] leading-none tracking-[0.01em] text-foreground transition-colors hover:bg-foreground hover:text-background"
                      >
                        {skill}
                      </li>
                    ))}
                  </ul>
                </div>
              ))}
            </div>
          </Reveal>
        </div>
      </div>
    </section>
  );
}

/* ------------------------------ beyond ------------------------------ */

function Beyond() {
  return (
    <section className="border-t border-line">
      <div className="mx-auto max-w-[1440px] px-6 py-24 md:px-10 md:py-32">
        <Reveal>
          <p className="eyebrow">Section 08 - Beyond the screen</p>
        </Reveal>
        <Reveal delay={100}>
          <h2 className="font-display mt-6 max-w-3xl text-[40px] leading-[1.02] md:text-[64px]">
            A quieter life,
            <br />
            <span className="italic text-muted-foreground">between the projects.</span>
          </h2>
        </Reveal>

        <div className="mt-20 grid grid-cols-12 gap-4 md:mt-28 md:gap-6">
          <Reveal className="col-span-8 md:col-span-5">
            <div className="aspect-[4/5] w-full overflow-hidden">
              <img src={beyondSketch} alt="Sketchbook" loading="lazy" className="h-full w-full object-cover" />
            </div>
            <p className="eyebrow mt-4">Sketchbooks</p>
          </Reveal>

          <div className="col-span-4 flex flex-col gap-4 md:col-span-3 md:gap-6">
            <Reveal delay={80}>
              <div className="aspect-square w-full overflow-hidden">
                <img src={beyondCoffee} alt="Morning coffee" loading="lazy" className="h-full w-full object-cover" />
              </div>
              <p className="eyebrow mt-3">Slow mornings</p>
            </Reveal>
            <Reveal delay={160}>
              <div className="aspect-[4/5] w-full overflow-hidden">
                <img src={beyondBooks} alt="Books" loading="lazy" className="h-full w-full object-cover" />
              </div>
              <p className="eyebrow mt-3">Reading</p>
            </Reveal>
          </div>

          <Reveal delay={120} className="col-span-12 md:col-span-4">
            <div className="aspect-[4/5] w-full overflow-hidden">
              <img src={beyondTravel} alt="Mountains" loading="lazy" className="h-full w-full object-cover grayscale" />
            </div>
            <p className="eyebrow mt-4">Weekends away</p>
          </Reveal>

          <Reveal className="col-span-7 md:col-span-7">
            <div className="aspect-[16/10] w-full overflow-hidden">
              <img src={beyondCampus} alt="Campus" loading="lazy" className="h-full w-full object-cover" />
            </div>
            <p className="eyebrow mt-4">Roorkee, at dusk</p>
          </Reveal>
          <Reveal delay={120} className="col-span-5 md:col-span-5">
            <div className="aspect-[16/10] w-full overflow-hidden">
              <img src={beyondEvents} alt="Events" loading="lazy" className="h-full w-full object-cover" />
            </div>
            <p className="eyebrow mt-4">Rooms full of ideas</p>
          </Reveal>
        </div>
      </div>
    </section>
  );
}

/* ------------------------------ footer ------------------------------ */

function Footer() {
  return (
    <footer id="contact" className="bg-[#0d0b09] text-[#f5f0e6]">
      <div className="mx-auto max-w-[1440px] px-6 py-24 md:px-10 md:py-32">
        <Reveal>
          <p className="text-[11px] font-medium uppercase tracking-[0.2em] text-[#f5f0e6]/50">
            Section 09 - Get in touch
          </p>
        </Reveal>
        <Reveal delay={120}>
          <h2 className="font-display mt-10 text-[52px] leading-[0.98] tracking-tight sm:text-[80px] md:text-[120px] lg:text-[152px]">
            Let&apos;s build
            <br />
            something
            <br />
            <span className="italic text-[#f5f0e6]/60">meaningful.</span>
          </h2>
        </Reveal>

        <div className="mt-24 grid grid-cols-1 gap-16 border-t border-[#f5f0e6]/15 pt-12 md:grid-cols-12">
          <Reveal className="md:col-span-5">
            <p className="text-[11px] font-medium uppercase tracking-[0.2em] text-[#f5f0e6]/50">
              Direct
            </p>
            <a
              href="mailto:zoya@example.com"
              className="font-display mt-4 block text-[28px] leading-tight md:text-[36px]"
            >
              <span className="link-underline-inner">zoya.shaikh@iitr.ac.in</span>
            </a>
            <p className="mt-6 max-w-xs text-[14px] leading-relaxed text-[#f5f0e6]/60">
              Currently open to product design, brand and strategy engagements
              for 2026. Small teams, considered problems.
            </p>
          </Reveal>

          <Reveal delay={100} className="md:col-span-4">
            <p className="text-[11px] font-medium uppercase tracking-[0.2em] text-[#f5f0e6]/50">
              Elsewhere
            </p>
            <ul className="mt-4 space-y-3 text-[16px]">
              {[
                ["LinkedIn", "https://www.linkedin.com/in/zoyashaikh-iitr"],
                ["Figma Portfolio", "#"],
                ["Read.cv", "#"],
                ["Instagram", "#"],
              ].map(([label, href]) => (
                <li key={label}>
                  <a href={href} className="group inline-flex items-center gap-3 text-[#f5f0e6]/85 transition-colors hover:text-[#f5f0e6]">
                    <span className="link-underline-inner">{label}</span>
                    <span aria-hidden className="translate-x-0 opacity-60 transition-all group-hover:translate-x-1">↗</span>
                  </a>
                </li>
              ))}
            </ul>
          </Reveal>

          <Reveal delay={200} className="md:col-span-3">
            <p className="text-[11px] font-medium uppercase tracking-[0.2em] text-[#f5f0e6]/50">
              Documents
            </p>
            <ul className="mt-4 space-y-3 text-[16px]">
              <li>
                <a href="#" className="link-underline-inner text-[#f5f0e6]/85 hover:text-[#f5f0e6]">Résumé - PDF</a>
              </li>
              <li>
                <a href="#" className="link-underline-inner text-[#f5f0e6]/85 hover:text-[#f5f0e6]">Case studies - PDF</a>
              </li>
              <li>
                <a href="#" className="link-underline-inner text-[#f5f0e6]/85 hover:text-[#f5f0e6]">Press kit</a>
              </li>
            </ul>
          </Reveal>
        </div>

        <div className="mt-24 flex flex-wrap items-end justify-between gap-6 border-t border-[#f5f0e6]/15 pt-8 text-[12px] text-[#f5f0e6]/50">
          <p>© 2026 Zoya Shaikh. Designed and built by hand, in Roorkee.</p>
          <p>Set in Instrument Serif &amp; Inter Tight. Vol. I.</p>
        </div>
      </div>
    </footer>
  );
}

